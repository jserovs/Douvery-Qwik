import { component$ } from '@builder.io/qwik';
import { BottonDisable } from '~/components/buttons/botton-disable/botton-disable';

export const CriptoUsdtMethod = component$(() => {
  return (
    <>
      <BottonDisable />
    </>
  );
});
